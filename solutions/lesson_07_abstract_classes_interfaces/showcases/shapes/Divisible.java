/**
 * Divisible Interface for the showcase
 * 
 * @author David Gemen
 */
public interface Divisible {

    /**
     * Divides someting by the divisor
     * 
     * @param divisor the divisor the object should be divided by
     */
    public void divide(int divisor);
}
